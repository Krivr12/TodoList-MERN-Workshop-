# Steps

1. Provision Nginx Lightsail instance
2. Connect with SSH
3. Change directory to `/opt/bitnami/nginx/html`
4. Remove all files inside this directory
5. Copy all HTML and CSS files to this directory
6. Visit the public ipv4 address of the instance `http://{ipv4-address}`
